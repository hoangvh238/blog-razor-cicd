@model ErrorViewModel
@{
    ViewData["Title"] = "Lỗi";
}

<div style="padding:20px">
    <h1 class="text-danger">Lỗi.</h1>
    <h2 class="text-danger">Đã xảy ra lỗi khi xử lý yêu cầu của bạn.</h2>

    @if (Model.ShowRequestId)
    {
        <p>
            <strong>ID Yêu Cầu:</strong> <code>@Model.RequestId</code>
        </p>
    }

    <h3>Chế Độ Phát Triển</h3>
    <p>
        Chuyển về chế độ <strong>Phát Triển</strong> sẽ cung cấp khả năng xem chi tiết lỗi.
    </p>
    <p>
        <strong>Chế độ phát triển nên bị tắt cho các ứng dụng đã xuất bản.</strong>
        Chế độ <strong>Phát Triển</strong> có thể được thiết lập bằng cách đặt biến môi trường <strong>ASPNETCORE_ENVIRONMENT</strong> thành <strong>Phát Triển</strong> và khởi động lại.
    </p>
</div>
