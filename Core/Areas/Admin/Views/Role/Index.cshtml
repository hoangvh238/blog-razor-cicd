@using EntityLayer.Concrete
@model List<Role>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox ">
                <div class="ibox-title">
                    <h3>Vai Trò</h3>
                </div>
                <div class="ibox-content table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Vai Trò</th>
                                <th style="text-align:center">Xóa</th>
                                <th style="text-align:center">Chỉnh Sửa</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th style="vertical-align:middle;">@item.Id</th>
                                    <td style="vertical-align:middle;">@item.Name</td>
                                    <td style="vertical-align:middle;" class="text-center">
                                        <button class="btn btn-danger" style="width:50px; padding: 8px 10px; font-size: 14px; text-align: center;"
                                                onclick="confirmDelete(@item.Id)">
                                            Xóa
                                        </button>
                                    </td>
                                    <td style="text-align:center; vertical-align:middle;">
                                        <a href="/Admin/Role/EditRole/@item.Id" class="btn btn-outline btn-success">Chỉnh Sửa</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="d-flex justify-content-center">
                <a href="/Admin/Role/AddRole/" class="btn btn-primary" style="width:150px; margin-right: 5px;">Thêm Vai Trò</a>
                <a href="/Admin/Role/UserRoleList/" class="btn btn-success" style="width:150px; margin-left: 5px;">Gán Vai Trò</a>
            </div>
        </div>
    </div>
</div>
<script>
    function confirmDelete(roleId) {
        Swal.fire({
            title: 'Xóa?',
            text: 'Bạn có chắc chắn muốn xóa vai trò này không?',
            showDenyButton: true,
            confirmButtonText: 'Có',
            denyButtonText: 'Không',
            customClass: {
                actions: 'my-actions',
                cancelButton: 'order-1 right-gap',
                confirmButton: 'order-2',
                denyButton: 'order-3',
            },
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = '/Admin/Role/DeleteRole/' + roleId;
            }
        })
    }
</script>
